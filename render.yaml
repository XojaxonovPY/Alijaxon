services:
  - type: web
    name: alijaxon
    env: docker
    plan: free
    branch: main
    region: oregon
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: "python3 manage.py makemigrations && python3 manage.py migrate && gunicorn root.wsgi:application"


